# Module 4: Document Intelligence

## Objective
Build a reusable document-processing pipeline that can extract and process different types of documents.

Use LlamaParse and n8n to extract structured content from PDFs, map the fields into NocoDB, and send a summary email. Validate the same pipeline with an invoice, a CV, and a form, then document when cloud or local LLMs are the better choice.